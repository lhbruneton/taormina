export enum GamePhase {
  InitialThrow = 'INITIAL THROW',
  InitialDraw = 'INITIAL DRAW',
  LoopThrow = 'LOOP THROW',
  LoopActions = 'LOOP ACTIONS',
  LoopDraw = 'LOOP DRAW',
  LoopExchange = 'LOOP EXCHANGE',
}
